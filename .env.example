# Optional. Unauthenticated requests work fine (60 req/hour) — every tool
# functions with zero setup. Set this only for a higher rate limit (5,000/hour).
# Create a token at https://github.com/settings/tokens (no scopes needed for public repos).
GITHUB_TOKEN=
